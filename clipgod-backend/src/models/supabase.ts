// Supabase types (generate via supabase gen types typescript --local)
export type Profile = { id: string; username: string; billing_plan: string; inactive_days: number };
export type Generation = { id: string; workspace_id: string; source_url: string; status: string; winner_clip: any; calendar: any; confetti_triggers: any };
// ... similarly for other tables
